Warning: you have no plugins providing access control for builds, so falling back to legacy behavior
of permitting any downstream builds to be triggered 
https://wiki.jenkins.io/display/JENKINS/Authorize+Project+plugin

Authorize Project
    jenkins > Configure Global Security
    Access Control for Builds
    Add > Project default Authorization
        Strategy - Run as User who Triggered Builds 

Parameterized trigger
